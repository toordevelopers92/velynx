name: publish

on:
  push:
    branches:
      - "main"
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          run_install: false
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          scope: "@toordevelopersinc"
          cache: "pnpm"
          cache-dependency-path: pnpm-lock.yaml
      - name: Verify npm token
        run: |
          if [ -z "${NODE_AUTH_TOKEN}" ]; then
            echo "NPM_TOKEN is missing. Add it in repository secrets."
            exit 1
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      - name: Show release versions
        run: |
          echo "Root: $(node -p 'require(\"./package.json\").version')"
          echo "Core: $(node -p 'require(\"./packages/core/package.json\").version')"
          echo "JS: $(node -p 'require(\"./packages/js/package.json\").version')"
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      - run: pnpm test
      - run: pnpm build
      - name: Publish core
        run: |
          VERSION=$(node -p "require('./package.json').version")
          if npm view "@toordevelopersinc/velynx@$VERSION" version > /dev/null 2>&1; then
            echo "Version $VERSION already published. Skipping."
          else
            npm publish --access public --provenance
          fi
        working-directory: packages/core
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      - name: Publish JS
        run: |
          VERSION=$(node -p "require('./package.json').version")
          if npm view "@toordevelopersinc/velynx-js@$VERSION" version > /dev/null 2>&1; then
            echo "Version $VERSION already published. Skipping."
          else
            npm publish --access public --provenance
          fi
        working-directory: packages/js
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
